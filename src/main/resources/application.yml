spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tupack?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC-5
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

application:
  security:
    jwt:
      secret: "cambia-esta-clave-super-secreta-y-larga-para-produccion-32+chars"
      access-token-expiration-minutes: 15
      refresh-token-expiration-days: 7